# 🤖 Telegram Bot Setup & Testing Guide

## ✅ Что уже готово:

### 1. База данных (Vercel Postgres)
- ✅ Схема БД создана (`lib/db/schema.sql`)
- ✅ Функции для работы с БД (`lib/db/index.ts`)
- ✅ Таблицы: `bookings`, `blocked_dates`, `settings`

### 2. API Endpoints
- ✅ `/api/booking` - Сохраняет бронирования в БД, отправляет уведомления
- ✅ `/api/available-slots` - Возвращает доступные слоты по дате и услуге
- ✅ `/api/telegram/webhook` - Обработчик для Telegram бота

### 3. Telegram Bot
- ✅ Обработчик кнопок (подтвердить/отклонить)
- ✅ Команды: `/start`, `/pending`, `/blocked`, `/block_date`, `/unblock_date`
- ✅ Автоматическая блокировка/разблокировка дат

### 4. Конфигурация услуг
- ✅ Все услуги по 1 часу, кроме "Nagelmodellage mit Gel" (2 часа)
- ✅ Слоты времени: 9:00-16:00

---

## 🚀 Шаги для запуска в продакшн:

### Шаг 1: Создать Vercel Postgres БД

1. Открой [Vercel Dashboard](https://vercel.com/dashboard)
2. Выбери проект `lena-schneider-fusspflege`
3. **Storage** → **Create Database** → **Postgres**
4. Регион: **Frankfurt** (ближе к Германии)
5. После создания → **Query** → выполни SQL из файла `lib/db/schema.sql`:

```sql
-- Copy entire content from lib/db/schema.sql
```

### Шаг 2: Создать Telegram бота

1. Открой Telegram → найди **@BotFather**
2. Отправь: `/newbot`
3. Название: **Lena Fußpflege Bot**
4. Username: `lena_fusspflege_bot` (или любой доступный)
5. Скопируй **токен** (например: `7895641230:AAHdqTcvCH1vGQRxP1Dw3FEiJxYQUKa-4-M`)

### Шаг 3: Узнать Chat ID для Лены

1. Напиши боту любое сообщение в Telegram (например: "Привет")
2. Открой в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
3. Найди в JSON: `"chat":{"id":123456789}` - это Chat ID
4. Запиши оба значения (токен и Chat ID)

### Шаг 4: Настроить Webhook

После деплоя на Vercel, выполни команду:

```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://fusspflege-lena-schneider.de/api/telegram/webhook"}'
```

Проверь webhook:
```bash
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
```

### Шаг 5: Добавить переменные на Vercel

1. Vercel → Project → **Settings** → **Environment Variables**
2. Добавь:

```env
TELEGRAM_BOT_TOKEN=7895641230:AAHdqTcvCH...
TELEGRAM_CHAT_ID=123456789
```

### Шаг 6: Включить Telegram уведомления

В файле `app/api/booking/route.ts` измени:

```typescript
// Было:
const TELEGRAM_DISABLED = true;

// Стало:
const TELEGRAM_DISABLED = false;
```

### Шаг 7: Деплой

```bash
git add .
git commit -m "Enable Telegram bot for production"
git push
```

---

## 🧪 Локальное тестирование (БЕЗ продакшн БД)

Для локального тестирования бота без Vercel Postgres используй **ngrok** для webhook:

### 1. Установи ngrok
```bash
brew install ngrok
# или скачай с https://ngrok.com
```

### 2. Запусти локальный сервер
```bash
npm run dev
# Сервер стартует на http://localhost:3005
```

### 3. Запусти ngrok
```bash
ngrok http 3005
```

Ngrok выдаст URL типа: `https://abc123.ngrok-free.app`

### 4. Настрой webhook на ngrok URL
```bash
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://abc123.ngrok-free.app/api/telegram/webhook"}'
```

### 5. Протестируй бота

Открой Telegram и отправь боту:
- `/start` - приветствие
- `/pending` - показать ожидающие бронирования
- `/blocked` - показать заблокированные даты
- `2025-12-24` - заблокировать дату
- `2025-12-24 Weihnachten` - заблокировать с причиной

---

## 📋 Команды бота для Лены

### `/start`
Приветствие и список доступных команд.

### `/pending`
Показывает все ожидающие подтверждения бронирования с кнопками:
- ✅ Bestätigen - подтвердить (отправит email клиенту)
- ❌ Ablehnen - отклонить

### `/blocked`
Показывает список всех заблокированных дат.

### `/block_date`
Инструкция по блокировке даты. После этого просто отправь:
- `2025-12-24` - заблокирует дату
- `2025-12-24 Urlaub` - заблокирует с причиной

### `/unblock_date`
Показывает заблокированные даты. Отправь дату для разблокировки:
- `2025-12-24` - разблокирует дату

### Умная блокировка/разблокировка
Просто отправь дату в формате `YYYY-MM-DD`:
- Если дата уже заблокирована → разблокирует
- Если дата свободна → заблокирует
- Можно добавить причину: `2025-12-31 Silvester`

---

## 🔄 Как работает система:

### 1. Клиент бронирует на сайте
```
Клиент → Форма на сайте → /api/booking
  ↓
Сохраняется в БД со статусом "pending"
  ↓
Отправляется уведомление в Telegram с кнопками
  ↓
Отправляется email клиенту (подтверждение получения заявки)
```

### 2. Лена получает уведомление
```
Telegram:
🆕 Neue Buchungsanfrage #123
👤 Max Mustermann
📞 +49 176 12345678
📧 max@example.com
💅 Smart Pediküre
📅 25. Dezember 2025
🕐 11:00 - 12:00

[✅ Bestätigen] [❌ Ablehnen]
```

### 3. Лена подтверждает/отклоняет
```
Лена нажимает кнопку:

✅ Подтвердить:
  - Статус → "confirmed"
  - Email клиенту: "Ihr Termin ist bestätigt!"
  - Слот становится недоступен на сайте

❌ Отклонить:
  - Статус → "rejected"
  - Email клиенту: "Termin nicht verfügbar"
```

### 4. Управление графиком
```
Лена блокирует дату:
  - Отправляет: "2025-12-24 Weihnachten"
  - Дата сохраняется в blocked_dates
  - Сайт не показывает слоты на эту дату
```

---

## 🎯 API Endpoints для фронтенда

### Получить доступные слоты
```bash
GET /api/available-slots?date=2025-12-15&service=Smart Pediküre

Response:
{
  "date": "2025-12-15",
  "service": "Smart Pediküre",
  "duration": "60 Min.",
  "availableSlots": [
    "09:00 - 10:00",
    "10:00 - 11:00",
    "13:00 - 14:00"
  ],
  "count": 3
}
```

### Для Nagelmodellage (2 часа)
```bash
GET /api/available-slots?date=2025-12-15&service=Nagelmodellage mit Gel

Response:
{
  "date": "2025-12-15",
  "service": "Nagelmodellage mit Gel",
  "duration": "120 Min.",
  "availableSlots": [
    "09:00 - 11:00",
    "13:00 - 15:00"
  ],
  "count": 2
}
```

---

## 🐛 Troubleshooting

### Telegram не получает уведомления:
1. Проверь webhook: `curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"`
2. Проверь переменные: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
3. Проверь `TELEGRAM_DISABLED = false` в `app/api/booking/route.ts`

### База данных не работает:
1. Убедись что создал БД на Vercel
2. Выполнил SQL из `lib/db/schema.sql`
3. Vercel автоматически добавит `POSTGRES_URL` в env

### Webhook ошибка:
```bash
# Удали старый webhook
curl -X POST "https://api.telegram.org/bot<ТОКЕН>/deleteWebhook"

# Установи новый
curl -X POST "https://api.telegram.org/bot<ТОКЕН>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url":"https://fusspflege-lena-schneider.de/api/telegram/webhook"}'
```

---

## ✨ Готово!

Теперь Лена управляет всем через Telegram! 🎉

Вопросы? Проблемы? Пиши мне! 😊
