---
import { MobileMenuToggle } from './react/MobileMenu';
---

<header
  id="site-header"
  class="fixed top-0 left-0 right-0 z-50 transition-all duration-300"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16 sm:h-20">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-2 group">
        <svg
          class="w-8 h-8 text-sage flex-shrink-0"
          viewBox="0 0 32 32"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M16 4C12 4 6 8 6 16c0 6 4 10 6 12 0-4 2-8 4-10 2 2 4 6 4 10 2-2 6-6 6-12C26 8 20 4 16 4z"
            fill="currentColor"
            opacity="0.85"
          />
          <path
            d="M16 10c-1 2-2 5-2 8M16 10c1 2 2 5 2 8"
            stroke="white"
            stroke-width="1.2"
            stroke-linecap="round"
            opacity="0.5"
          />
        </svg>
        <span class="hidden sm:block font-display text-olive text-lg sm:text-xl">
          Lena Schneider
        </span>
      </a>

      <!-- Desktop Navigation (center) -->
      <nav class="hidden lg:flex items-center gap-8">
        <a
          href="#services"
          class="nav-link font-body text-text-muted hover:text-sage transition-colors duration-200 text-sm tracking-wide"
          data-section="services"
        >
          Leistungen
        </a>
        <a
          href="#about"
          class="nav-link font-body text-text-muted hover:text-sage transition-colors duration-200 text-sm tracking-wide"
          data-section="about"
        >
          &Uuml;ber uns
        </a>
        <a
          href="#contact"
          class="nav-link font-body text-text-muted hover:text-sage transition-colors duration-200 text-sm tracking-wide"
          data-section="contact"
        >
          Kontakt
        </a>
        <a
          href="#booking"
          class="nav-link font-body text-text-muted hover:text-sage transition-colors duration-200 text-sm tracking-wide"
          data-section="booking"
        >
          Termin buchen
        </a>
      </nav>

      <!-- Desktop CTA (right) -->
      <div class="hidden lg:flex items-center gap-4">
        <a
          href="tel:+4917634237368"
          class="flex items-center gap-2 font-body text-text-muted hover:text-sage transition-colors duration-200 text-sm"
          aria-label="Anrufen"
        >
          <svg
            class="w-4 h-4"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z" />
          </svg>
          <span class="hidden xl:inline">0176 3423 7368</span>
        </a>
        <a
          href="#booking"
          class="bg-terracotta hover:bg-terracotta-dark text-white rounded-organic-sm px-5 py-2.5 font-body font-bold text-sm transition-colors duration-200"
        >
          Termin buchen
        </a>
      </div>

      <!-- Mobile Controls -->
      <div class="flex lg:hidden items-center gap-3">
        <a
          href="tel:+4917634237368"
          class="flex items-center justify-center w-10 h-10 text-olive hover:text-sage transition-colors duration-200"
          aria-label="Anrufen"
        >
          <svg
            class="w-5 h-5"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z" />
          </svg>
        </a>
        <MobileMenuToggle client:load />
      </div>
    </div>
  </div>
</header>

<script>
  // Scroll detection: add .scrolled class for background transition
  const header = document.getElementById('site-header');

  function handleScroll() {
    if (!header) return;
    if (window.scrollY > 20) {
      header.classList.add('scrolled');
    } else {
      header.classList.remove('scrolled');
    }
  }

  window.addEventListener('scroll', handleScroll, { passive: true });
  handleScroll();

  // IntersectionObserver for active nav link highlighting
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('.nav-link[data-section]');

  if (sections.length > 0 && navLinks.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const id = entry.target.id;
            navLinks.forEach((link) => {
              const section = link.getAttribute('data-section');
              if (section === id) {
                link.classList.add('text-sage', 'font-semibold');
                link.classList.remove('text-text-muted');
              } else {
                link.classList.remove('text-sage', 'font-semibold');
                link.classList.add('text-text-muted');
              }
            });
          }
        });
      },
      {
        rootMargin: '-20% 0px -60% 0px',
        threshold: 0,
      }
    );

    sections.forEach((section) => observer.observe(section));
  }
</script>

<style>
  #site-header {
    background: transparent;
  }

  #site-header.scrolled {
    background: rgba(253, 246, 236, 0.9);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    box-shadow: 0 1px 3px rgba(74, 85, 64, 0.08);
  }
</style>
